<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="28"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo_invoice"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_invoice"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.QuickInvoiceMobile"
        tools:targetApi="31">
        <activity
            android:name=".main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".features.login.LoginActivity"/>
        <activity android:name=".features.register.RegisterActivity"/>
        <activity android:name=".features.dashboard.DashboardActivity"/>
        <activity android:name=".features.invoice.CreateInvoiceActivity"/>
        <activity android:name=".features.invoice.InvoiceDetailActivity"/>
        <activity android:name=".features.invoice.InvoicePreviewActivity"/>
        <activity android:name=".features.profile.ProfileActivity"/>
        <activity android:name=".features.subscription.SubscriptionActivity"/>
    </application>

</manifest>